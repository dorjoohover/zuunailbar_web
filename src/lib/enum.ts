export enum TeamType {
  TEAM = 10,
  GROUP = 20,
  PARTNER = 30,
}

export enum DISCOUNT_TYPE {
  PERCENT = 10,
  PRICE = 20,
}

export enum SCHEDULE_STATUS {
  ACTIVE = 10,
  EXCUSED = 20,
  VACATION = 30,
  ABSENT = 40,
}
