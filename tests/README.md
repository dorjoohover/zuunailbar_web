# Web Tests

Frontend automated tests are not installed yet, but this folder is ready for them.

## Planned Layout

- `unit/`: utility and hook tests
- `component/`: isolated UI tests
- `e2e/`: customer booking and payment flows
- `manual/`: scenario files and temporary QA notes

Recommended future stack: Vitest for unit/component tests and Playwright for e2e.
